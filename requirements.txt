# Core logic & env
python-dotenv==1.0.1
tqdm==4.66.2
requests==2.31.0

# Streamlit UI
streamlit==1.32.2

# Notion client
notion-client==2.2.1     # Updated to latest stable version as of April 2025

# LangChain ecosystem (LangChain v0.3-compatible stack)
langchain==0.3.0
langchain-community==0.3.0     # Updated to match langchain version
langchain-groq==0.1.3
langchain-ollama==0.1.3        # Updated to latest compatible version
langgraph==0.2.3              # Updated to more recent stable version
langchain-qdrant==0.1.2       # Updated to latest compatible version
langchain-huggingface==0.0.3

# Embeddings + RAG
sentence-transformers==3.0.1  # Updated to latest stable version
transformers==4.40.1          # Updated to more recent stable version

# Vector DB
qdrant-client==1.9.1          # Updated to latest stable version

# Torch (CPU version)
torch==2.2.2                  # Updated to latest patch version
--extra-index-url https://download.pytorch.org/whl/cpu

# Optional
beautifulsoup4==4.12.3