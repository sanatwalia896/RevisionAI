# Core logic & environment
python-dotenv
tqdm
requests

# Streamlit UI
streamlit>=1.25.0

# Notion client
notion-client==2.2.0

# LangChain ecosystem
langchain>=0.1.0
langchain_community
langchain_groq
langchain_ollama
langgraph
langchain-qdrant

# Embedding + RAG
sentence-transformers>=2.2.2

# Vector DB
qdrant-client>=1.6.0

# Optional: for MongoDB (you mentioned but not currently used)
# pymongo>=4.3.3

# Optional parsing support (for future PDF/HTML enhancements)
beautifulsoup4

# Optional: only include if you're *actually* using TensorFlow
# tensorflow==2.14.0

# Optional: FAISS only if you switch to it (not needed for Qdrant)
# faiss-cpu
